<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @project.title %>
</p>

<h2>Bugs</h2>
<%= render @project.bugs %>

<h2>Report a bug:</h2>
<%= render 'bugs/form' %>

<%= link_to 'Edit', edit_project_path(@project) %> |
<%= link_to 'Back', projects_path %>
